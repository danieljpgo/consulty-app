export interface Form {
  subject: string;
  daysOfWeek: string;
  time: string;
}

export interface Teacher {
  avatar: string;
  bio: string;
  cost: number;
  id: number;
  name: string;
  subject: string;
  user_id: number;
  whatsapp: string;
}
